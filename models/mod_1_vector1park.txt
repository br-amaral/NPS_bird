# two part model - occupancy at the two different levels
# detection covariates

model {	
  # Priors
  for(i in 1:n_spsM){	     # species i
    
    alpha0[i] ~ dnorm(mu.alpha0, tau.alpha0)	

    for(t in 1:nyrM) {
      beta0[i,t] ~ dnorm(mu.beta0, tau.beta0)	
    }
    
    for(j in 1:n_bs){     # number of betas
      beta[j,i] ~ dnorm(mu.beta[j], tau.beta[j])
    }

    for(j in 1:n_as){     # number of alphas
      alpha[j,i] ~ dnorm(mu.alpha[j], tau.alpha[j])
    }
  }
  
  # Hyperpriors for community hyperparameters - hey put this in loops.	
  # Ecological process	
  
  mu.beta0 ~ dnorm(0, 0.36)
  tau.beta0 ~ dgamma(0.1,0.1) 
  
  for(j in 1:n_bs){     # number of betas
    mu.beta[j] ~ dnorm(0, 0.36)
    tau.beta[j] ~ dgamma(0.1,0.1)
  }
  
  # Sampling process	
  mu.alpha0 ~ dnorm(0, 0.36)
  tau.alpha0 ~ dgamma(0.1,0.1) 
  
  for(j in 1:n_as){     # number of alphas
    mu.alpha[j] ~ dnorm(0, 0.36)
    tau.alpha[j] ~ dgamma(0.1,0.1)
  }
  
  ## super vector loop -------------------------------
  for(a in 1:nrowy){	                
    # Occupancy	
    Z[y[a,8],y[a,4],y[a,5]] ~ dbern(psi[y[a,8],y[a,4],y[a,5]])	
    
    # Covariate effects on the ecological process
    logit(psi[y[a,8],y[a,4],y[a,5]]) <-      # spskey_p, parkey, site_n, year_n
      beta0[y[a,8],y[a,5]] +                 # spskey_p, year_n
      # site level
      beta[1,y[a,8]] * X[a,1] +
      beta[2,y[a,8]] * X[a,2] +
      # park level
      beta[3,y[a,8]] * X[a,3] +
      beta[4,y[a,8]] * X[a,4] +
      # county level
      beta[5,y[a,8]] * X[a,5] +
      beta[6,y[a,8]] * X[a,6] +
      # park size
      beta[7,y[a,8]] * X[a,5]
    
    logit(p[y[a,8],y[a,4],y[a,5],y[a,6]]) <-       # spskey_p, site_n, year_n, interval_n
      alpha0[y[a,8]] +                  # intercept - spskey_p
      alpha[1,y[a,8]] * X[a,8] +        # day - spskey
      alpha[2,y[a,8]] * (X[a,8])^2 +    # day squared - spskey,
      alpha[3,y[a,8]] * X[a,9]          # time - spskey
    
    y[a,] ~ dbern(p[y[a,8],y[a,4],y[a,5],y[a,6]] * Z[y[a,8],y[a,4],y[a,5]])
  }
}
